#data "terraform_remote_state" "vpc" {
#   backend = "local" # "s3"
  # config = {
  #   bucket  = var.backend_bucket
  #   key     = "crst-ecr.tfstate"  #vpc.state
  #   region  = var.region
  #   profile = var.aws_profile
  #}
#}